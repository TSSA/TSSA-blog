<h1> <%=@post.title%> </h1>
<small> <%=@post.created_at.strftime("%b %d. %Y") %> </small>
<p> <%= @post.body %> </p>
<small>Category: <%= link_to @post.category.name, category_path(:id => @post.category.id) %> </small>
<br>
<%= link_to "Back", posts_path %>
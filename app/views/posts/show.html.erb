<div class="container">
<div class="post">
  <h1> <%=@post.title%> </h1> <small> <%=@post.created_at.strftime("%b %d. %Y") %> </small>|<small> Category: <%= link_to @post.category.name, category_path(:id => @post.category.id) %> </small>
<p> <%= @post.body.html_safe %> </p>
<%= link_to "Back", posts_path %>
</div>
</div>
